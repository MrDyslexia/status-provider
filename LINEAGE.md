# Lineage

`status-provider` is an independent clean-history project created from prior fork development work.

The previous fork remains useful as historical reference, but this repository owns its package identity, command names, configuration paths, documentation, release process, and validation environment.

Upstream changes may be reviewed manually when useful, but they are not merged automatically.
